# Application Settings
NODE_ENV=development
PORT=5000

# MongoDB Database Connection String (Atlas or Local)
MONGODB_URI=mongodb+srv://computergoogl70_db_user:saad1234@cluster1.lwkncel.mongodb.net/animedb?retryWrites=true&w=majority

# JWT Credentials
JWT_SECRET=change_this_to_a_long_random_string_min_32_chars
JWT_EXPIRES_IN=7d

# CORS Client Allowed Origin (Set to Vercel URL in production: e.g. https://your-app.vercel.app)
CLIENT_URL=http://localhost:5173

# Consumet (@consumet/extensions) — episode import & live m3u8 resolution
# Providers: AnimeSaturn, AnimeUnity (recommended for playback)

